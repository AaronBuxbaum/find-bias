type TwitterUser {
  id: ID! @id
  handle: String! @unique
  tweets: [Tweet!]! @relation(link: INLINE)
  statuses_count: Int
  name: String
}

type Tweet {
  id: ID! @id
  twitterId: String! @unique
  handle: TwitterUser!
  text: String!
}
