type TwitterUser {
  id: ID! @id
  handle: String! @unique
  tweets: [Tweet!]!
  statuses_count: Int
  name: String
}

type Tweet {
  id: ID! @id
  twitterId: Float! @unique
  twitterIdString: String! @unique
  handle: TwitterUser! @relation(link: INLINE)
  text: String!
}
